import AddFriend from "@/components/AddFriend";
import { FC } from "react";

interface pageProps {}

const page: FC<pageProps> = ({}) => {
  return (
    <div>
      <AddFriend />
    </div>
  );
};

export default page;
